---
import type { Listing } from '../data/listings';
import { getListingImages } from '../data/listings';

interface Props {
  listing: Listing;
  index?: number;
}

const { listing, index = 0 } = Astro.props;
const images = getListingImages(listing);
---

<a
  href={`/listings/${listing.id}`}
  class="group block bg-white rounded-sm overflow-hidden shadow-sm hover:shadow-xl transition-all duration-500 hover:-translate-y-1"
>
  <!-- Image Header -->
  <div class="relative h-48 overflow-hidden bg-earth-200">
    <img
      src={images[0]}
      alt={listing.title}
      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
      loading="lazy"
    />
    <div class="absolute inset-0 bg-gradient-to-t from-earth-900/50 to-transparent"></div>
    <div class="absolute inset-0 flex items-end p-5">
      <div>
        <span class="inline-block px-2.5 py-1 bg-white/15 backdrop-blur-sm rounded-sm text-[11px] uppercase tracking-widest text-white/90 font-semibold mb-2">
          {listing.county}
        </span>
        <p class="text-2xl font-display text-white leading-tight">{listing.acreage}</p>
      </div>
    </div>
    <div class="absolute top-4 right-4 flex items-center gap-2">
      {images.length > 1 && (
        <span class="inline-flex items-center gap-1 px-2.5 py-1 bg-earth-900/50 backdrop-blur-sm rounded-sm text-[11px] text-white/80">
          <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 15.75l5.159-5.159a2.25 2.25 0 013.182 0l5.159 5.159m-1.5-1.5l1.409-1.41a2.25 2.25 0 013.182 0l2.909 2.91M3.75 21h16.5a2.25 2.25 0 002.25-2.25V5.25a2.25 2.25 0 00-2.25-2.25H3.75a2.25 2.25 0 00-2.25 2.25v13.5A2.25 2.25 0 003.75 21z" />
          </svg>
          {images.length}
        </span>
      )}
      <span class="inline-block px-3 py-1 bg-white/20 backdrop-blur-sm rounded-sm text-xs font-semibold text-white tracking-wide">
        {listing.status === 'available' ? 'Available' : listing.status === 'pending' ? 'Pending' : 'Sold'}
      </span>
    </div>
  </div>

  <!-- Content -->
  <div class="p-5">
    <h3 class="font-display text-lg text-earth-900 leading-snug mb-2 group-hover:text-forest-700 transition-colors duration-300">
      {listing.title}
    </h3>
    <p class="text-sm text-earth-600 leading-relaxed line-clamp-2 mb-4">
      {listing.description}
    </p>
    <div class="flex items-center justify-between pt-3 border-t border-earth-100">
      <span class="font-display text-xl text-forest-700">{listing.price}</span>
      <span class="text-xs text-earth-500 flex items-center gap-1 group-hover:text-copper-500 transition-colors">
        View Details
        <svg class="w-3.5 h-3.5 transform group-hover:translate-x-1 transition-transform duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
        </svg>
      </span>
    </div>
  </div>
</a>
