---
import Layout from '../layouts/Layout.astro';
import { team } from '../data/team';
export const prerender = true;
---

<Layout title="About Us — Julie Wright Land Company" description="Meet the experienced team behind Julie Wright Land Company. Over 40 years of Triangle area land expertise.">
  <!-- Page Header -->
  <section class="relative py-20 lg:py-24 bg-earth-900 overflow-hidden">
    <img src="/FallsLakeTopoContours.svg" class="topo-bg opacity-[0.09]" style="filter: invert(1);" alt="" aria-hidden="true" />
    <div class="relative max-w-7xl mx-auto px-6 lg:px-8">
      <p class="section-label text-white/70">Our Story</p>
      <h1 class="font-display text-4xl md:text-6xl text-earth-50 leading-tight mt-3">
        About <span class="italic text-[#faca83]">Our Team</span>
      </h1>
      <p class="text-earth-400 mt-5 max-w-2xl text-lg">
        A dedicated team of brokers and developers with deep roots in North Carolina's Triangle region.
      </p>
    </div>
  </section>

  <!-- Company Story -->
  <section class="py-20 lg:py-28">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
      <div class="grid lg:grid-cols-5 gap-16">
        <div class="lg:col-span-3">
          <p class="section-label">Since 1984</p>
          <h2 class="section-heading">Four Decades of<br />Land Expertise</h2>
          <div class="mt-8 space-y-5 text-earth-700 leading-relaxed">
            <p>
              Julie Wright Land Company has been the Triangle's trusted land brokerage for over 40 years. Founded by Julie Wright — a registered nurse turned real estate visionary — the company was built on a simple premise: helping people find the right piece of land, or the right use for their land.
            </p>
            <p>
              That means understanding not just the market, but the land itself. Soil composition. Topography. Zoning regulations. Access considerations. Tax implications. Estate planning. We navigate all of it so our clients can make informed, confident decisions.
            </p>
            <p>
              Today, our team of five experienced brokers covers eight counties — Wake, Durham, Granville, Franklin, Vance, Harnett, Nash, and Chatham — with the kind of deep local knowledge that only comes from decades of relationships and transactions in these communities.
            </p>
          </div>
        </div>

        <!-- Sidebar stats -->
        <div class="lg:col-span-2 space-y-6">
          {[
            { label: 'Years in Business', value: '40+', detail: 'Founded in 1984' },
            { label: 'Counties Covered', value: '8', detail: 'Across the Triangle' },
            { label: 'Team Members', value: '5', detail: 'Experienced Brokers' },
            { label: 'Core Values', value: '—', detail: 'Integrity, Knowledge, Relationships' },
          ].map((stat) => (
            <div class="bg-earth-100/60 border border-earth-200 rounded-sm p-6">
              <p class="text-xs uppercase tracking-[0.2em] text-[#be3528] font-semibold">{stat.label}</p>
              <p class="font-display text-3xl text-earth-900 mt-1">{stat.value !== '—' ? stat.value : ''}</p>
              <p class="text-sm text-earth-600 mt-1">{stat.detail}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Team -->
  <section class="py-20 lg:py-28 bg-earth-100/50 border-t border-earth-200" id="team">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
      <div class="text-center mb-16">
        <p class="section-label">The Team</p>
        <h2 class="section-heading">Meet Our Brokers</h2>
      </div>

      <div class="space-y-8">
        {team.map((member, i) => (
          <div class="bg-white rounded-sm border border-earth-100 shadow-sm overflow-hidden" id={member.name.toLowerCase().replace(/\s/g, '-')}>
            <div class="p-8 lg:p-10">
              <div class="flex flex-col md:flex-row md:items-start gap-8">
                <img
                  src={member.photo}
                  alt={member.name}
                  class="w-24 h-24 rounded-full object-cover flex-shrink-0 border-2 border-earth-200 shadow-sm"
                  loading="lazy"
                />

                <div class="flex-1">
                  <div class="flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-4">
                    <h3 class="font-display text-2xl text-earth-900">{member.name}</h3>
                    <span class="text-xs uppercase tracking-widest text-[#be3528] font-semibold">{member.role}</span>
                  </div>
                  <p class="text-earth-600 leading-relaxed mt-4">{member.bio}</p>

                  <div class="flex flex-wrap gap-2 mt-5">
                    {member.specialties.map((s) => (
                      <span class="px-3 py-1 bg-earth-100 text-earth-600 text-xs font-medium rounded-sm">{s}</span>
                    ))}
                  </div>

                  <div class="flex flex-wrap items-center gap-6 mt-5 pt-5 border-t border-earth-100">
                    <a href={`tel:${member.phone.replace(/\D/g, '')}`} class="text-sm text-earth-600 hover:text-crimson-700 transition-colors flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 6.75c0 8.284 6.716 15 15 15h2.25a2.25 2.25 0 002.25-2.25v-1.372c0-.516-.351-.966-.852-1.091l-4.423-1.106c-.44-.11-.902.055-1.173.417l-.97 1.293c-.282.376-.769.542-1.21.38a12.035 12.035 0 01-7.143-7.143c-.162-.441.004-.928.38-1.21l1.293-.97c.363-.271.527-.734.417-1.173L6.963 3.102a1.125 1.125 0 00-1.091-.852H4.5A2.25 2.25 0 002.25 4.5v2.25z" />
                      </svg>
                      {member.phone}
                    </a>
                    <a href={`mailto:${member.email}`} class="text-sm text-earth-600 hover:text-crimson-700 transition-colors flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
                      </svg>
                      {member.email}
                    </a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-14 bg-earth-900">
    <div class="max-w-3xl mx-auto px-6 lg:px-8 text-center">
      <h2 class="font-display text-3xl text-earth-50">Work With Our Team</h2>
      <p class="text-earth-400 mt-4">
        Ready to discuss your land needs? We'd love to hear from you.
      </p>
      <a href="/contact" class="btn-primary mt-8">Get in Touch</a>
    </div>
  </section>
</Layout>

